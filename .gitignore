__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
*.tmp

# Rainman's OWN dogfood memory stays out of this PUBLIC repo — it holds local
# absolute paths and dev-chatter, not something to publish. (The "git-committable
# project layer" is a feature for users' PRIVATE/team repos, not a mandate to
# commit this repo's own store.) Private marketing/ops live in rainman-private.
.rainman/
