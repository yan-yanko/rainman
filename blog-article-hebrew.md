<div dir="rtl">

# איך גרמתי ל-AI לזכור מה בניתי — בלי לשלם טוקן אחד על זה

**כלי זיכרון לפיתוח שנולד מתוך תסכול אמיתי, עובד לוקאלית, ומשתלב עם כל סביבת AI שתרצו.**

---

## הרגע שבו שברתי

לפני כמה חודשים ישבתי על פרויקט חיזוי בחירות — מודל שמנתח הטיה בסקרים. סשן ארוך עם Claude Code, שעות של דיבאגינג, הכל מתקדם יפה. ואז ה-context התמלא, Claude ביצע compaction, והכל — פשוט נעלם.

התחלתי להסביר מחדש מה הבעיה. Claude הציע לי לכתוב מודול לחישוב הטיה. אמרתי לו שכבר יש מודול כזה. הוא לא מצא אותו. ביקשתי שיחפש שוב. לא מצא.

המודול היה שם. 516 שורות. עובד. נבדק. Claude פשוט שכח שהוא קיים.

הלכו שעתיים על לא כלום. לא בגלל באג בקוד, לא בגלל בעיה לוגית — בגלל שה-AI שכח מה כבר בנינו ביחד.

אם אתם עובדים עם כלי AI לפיתוח — Claude Code, Cursor, Copilot, Cline — אתם מכירים את הרגע הזה. זה לא קורה פעם. זה קורה כל יום.

---

## הבעיה האמיתית: Context Rot

זה לא באג. ככה LLMs עובדים.

לכל מודל יש חלון הקשר — context window — שמוגבל בגודלו. ככל שהשיחה מתארכת, תוכן ישן נדחס או נמחק כדי לפנות מקום לחדש. ההחלטות שקיבלתם בשעה הראשונה? הגישה שפסלתם? הקובץ שגילתם שהוא הגורם לבעיה? הכל יכול להיעלם.

המונח שאני משתמש בו הוא **Context Rot** — ריקבון הקשר. המידע היה שם, עבד בשבילכם, ופתאום הוא לא.

הפתרון הנפוץ? קבצי `CLAUDE.md`. מסמכי handoff ידניים. העתק-הדבק של הקשר בתחילת כל סשן. יש אנשים שמנהלים מסמך Google Drive שהם מדביקים לתוך כל שיחה חדשה.

כל זה ידני. כל זה שביר. וכל זה מבזבז בדיוק את מה שכלי ה-AI היו אמורים לחסוך — זמן.

על פי נתונים מדווחים מקהילות מפתחים, כ-92% מההקשר נאבד אחרי 4 שעות עבודה (נתון לדוגמה — המספר המדויק משתנה בין כלים). כ-40% מהמפתחים שעובדים עם AI מציינים אובדן הקשר כנקודת הכאב המרכזית שלהם. זה לא בעיה שולית — זו הבעיה.

---

## מה זה Rainman?

Rainman הוא שכבת זיכרון קבועה לכלי פיתוח מבוססי AI.

בלי LLM. בלי קריאות API חיצוניות. בלי תלויות. רק Python סטנדרטי, רץ לוקאלית על המחשב שלכם.

הרעיון פשוט: במקום ש-context ילך לאיבוד כשסשן נגמר או נדחס, Rainman שומר את מה שחשוב — החלטות, כשלונות, פתרונות, תבניות, קונבנציות — כ"זיכרונות" מובנים. כשה-AI צריך את המידע, Rainman מחזיר אותו בדיוק ברגע הנכון.

**מה הוא שומר:**
- `decision` — "החלטנו להשתמש ב-Redis לסשנים ולא ב-JWT"
- `failure` — "ניסינו connection pooling עם 50 חיבורים, גרם ל-deadlock"
- `solution` — "הבאג ב-auth נפתר כשהוספנו retry עם exponential backoff"
- `pattern` — "כל הקריאות ל-API החיצוני עוברות דרך services/external.py"
- `convention` — "בפרויקט הזה אנחנו לא משתמשים ב-ORM, רק SQL ישיר"

**איך הוא מחליט מה רלוונטי:** מנגנון דירוג (scoring) מבוסס 4 רכיבים — התאמת מילות מפתח, עדכניות, חשיבות קטגורית, וקישורים אסוציאטיביים בין זיכרונות. אין פה הזיות של LLM. אין תוצאות "יצירתיות". רק התאמה מתמטית.

**איפה הוא עובד:** כל כלי שתומך ב-MCP — Claude Code, VS Code Copilot, Cursor, Cline, ועוד.

---

## ההתקנה — 3 שלבים, דקה אחת

```bash
pip install git+https://github.com/yan-yanko/rainman.git
rainman setup
rainman doctor
```

זהו.

`rainman setup` עושה את הכל אוטומטית:
- רושם את שרת ה-MCP של Rainman ב-Claude Code
- מתקין hooks שנדלקים אוטומטית בתחילת סשן, אחרי compaction, ובסוף סשן
- מגדיר אינטגרציה עם VS Code אם הוא מותקן

`rainman doctor` בודק שהכל תקין — שה-hooks מותקנים, ששרת ה-MCP נגיש, שהתיקיות במקום. אם משהו לא בסדר, הוא אומר בדיוק מה.

```bash
$ rainman doctor
[OK] Python 3.10+
[OK] rainman package installed
[OK] MCP server registered
[OK] Hooks installed (3/3)
[OK] Global store: ~/.rainman/memories.json
[OK] Project store: .rainman/memories.json
All checks passed.
```

---

## הפיצ'ר שמשנה הכל — Compaction Recovery

זה מה שגרם לי לבנות את הכלי הזה מלכתחילה.

כש-Claude Code מבצע compaction — כלומר דוחס את ההקשר כי חלון השיחה התמלא — Rainman מזהה את זה אוטומטית. הוא לא צריך שתגידו לו. ה-hook `session_start` נדלק עם matcher של `compact`, קורא את השיחה האחרונה, שולף זיכרונות רלוונטיים ממה שנשמר, ומזריק אותם בחזרה ל-context.

ה-AI ממשיך מאיפה שהפסיק. בלי שתסבירו מחדש. בלי שתדביקו קבצים. בלי שתעלו את ה-CLAUDE.md שלכם.

במקרה של פרויקט חיזוי הבחירות שסיפרתי עליו — אם Rainman היה מותקן, הוא היה שומר את ה-memory הזה:

```
category: solution
content: "election bias module exists at models/bias_correction.py (516 lines) —
          handles polling bias, historical adjustment, confidence intervals"
file_refs: ["models/bias_correction.py"]
importance: 0.8
```

וכש-Claude היה מבצע compaction ומנסה להציע "בוא נבנה מודול לחישוב הטיה" — Rainman היה מזריק את הזיכרון הזה ו-Claude היה יודע שהמודול כבר קיים. שעתיים נחסכות.

---

## שפה טבעית — "אל תשכח"

לא חייבים להשתמש בפקודות. אפשר פשוט לדבר.

כשאתם כותבים בשיחה עם Claude משהו כמו:

> "ה-auth service משתמש ב-Redis לטוקנים של סשנים, אל תשכח את זה"

Rainman תופס את זה בסוף הסשן דרך ה-hook `session_end`. הוא סורק את תמלול השיחה, מזהה אמירות שמסומנות כהחלטות או ידע חשוב, ושומר אותן כזיכרונות.

אפשר גם להשתמש בכלי MCP ישירות מתוך השיחה:

> "remember that the payment API has a 100ms timeout and we handle it with retry in services/payment.py"

וזה נשמר מיידית, עם קטגוריה, חשיבות, וקישורים אוטומטיים לזיכרונות קשורים.

---

## מה מבדיל את Rainman

יש כלים אחרים שמנסים לפתור את בעיית ה-context. מה שונה פה:

**אפס קריאות LLM.** הזיכרון לא "מתפרש" על ידי מודל שפה. הוא נשמר כמו שהוא, ומדורג על ידי מתמטיקה פשוטה. אין הזיות. אין עלות טוקנים על אחסון ודירוג. הטוקנים הנצרכים היחידים הם כשזיכרון מוזרק בפועל ל-context — כלומר רק כשהוא באמת שימושי.

**אפס תלויות חיצוניות.** רק Python stdlib. לא צריך `pip install` של 47 חבילות. לא צריך Docker. לא צריך שרת. לא צריך חשבון.

**נעילת קבצים (file locking).** כשה-hooks וה-MCP server רצים במקביל — ויש מצבים שזה קורה — Rainman משתמש ב-lockfile כדי למנוע התנגשויות. אטומיות מלאה: כתיבה דרך קובץ זמני + `os.replace`.

**צנזור סודות (secret redaction).** Rainman לא ישמור מפתחות AWS, GitHub tokens, או API keys. הוא סורק תוכן לפני שמירה ומחליף דפוסים רגישים ב-`[REDACTED]`. קבצים כמו `.env` ו-`*.pem` נדלגים לגמרי.

**כל MCP client.** לא רק Claude Code. כל כלי שתומך בפרוטוקול MCP יכול להשתמש ב-Rainman.

**130 טסטים.** 8 קבצי בדיקה, רצים תוך פחות מ-3 שניות. הכל עובד, הכל בדוק.

---

## איך הדירוג עובד

כשאתם שואלים את Rainman "מה אני יודע על ה-auth service?" — הוא לא מחפש מחרוזת. הוא מריץ מנגנון דירוג מרובע:

| רכיב | משקל | מה הוא עושה |
|-------|-------|-------------|
| **התאמת מילות מפתח** | 0.35 | חפיפה בין המילים בשאילתה לבין תוכן הזיכרון, תגיות, וקבצים |
| **עדכניות (temporal decay)** | 0.25 | זיכרונות טריים מקבלים ציון גבוה יותר, עם דעיכה לפי מודל ACT-R |
| **חשיבות לפי קטגוריה** | 0.20 | כשלון = 0.9, פתרון = 0.8, החלטה = 0.7 |
| **קישורים אסוציאטיביים** | 0.20 | זיכרונות שמקושרים לתוצאות מובילות מקבלים boost |

האחזור עובד בשני שלבים: קודם דירוג בלי הרכיב האסוציאטיבי, אחר כך דירוג מחדש עם boost לזיכרונות שמקושרים לתוצאות הכי טובות. זיכרונות ברמת הפרויקט מקבלים מכפלה של 1.2 על פני זיכרונות גלובליים.

השמירה עצמה היא JSON רגיל. שתי שכבות:

```
~/.rainman/              # שכבה גלובלית — חוצת פרויקטים
  memories.json

<project>/.rainman/      # שכבה פרויקטלית — אפשר לעשות git commit
  memories.json
```

כן, אפשר לעשות commit ל-`.rainman/` של הפרויקט ולשתף זיכרון עם הצוות. מפתח חדש שמצטרף לפרויקט מקבל את כל ההיסטוריה של ההחלטות, הכשלונות, והקונבנציות — מיידית.

---

## מה Rainman הוא לא

אני רוצה להיות כנה לגבי מה הכלי הזה. הנה מה שהוא **לא**:

- **הוא לא Confluence.** הוא לא מערכת ניהול ידע ארגונית. אין ממשק ווב מרהיב. אין חיפוש פנים-ארגוני.
- **הוא לא מחליף CLAUDE.md.** קובץ הוראות לפרויקט עדיין שימושי. Rainman מוסיף שכבה של זיכרון דינמי מעליו.
- **הוא לא קסם.** אם לא עבדתם על הפרויקט עם AI — אין מה לזכור. הערך גדל ככל שמשתמשים יותר.
- **הוא לא cloud.** הכל רץ לוקאלית. אין שרת, אין sync בין מחשבים (אלא אם כן אתם עושים git push לתיקיית `.rainman/`).

---

## לסיכום

בניתי את Rainman כי נמאס לי לחזור על עצמי. נמאס לי להסביר לעוזר ה-AI שלי מה כבר בנינו ביחד. נמאס לי ששעות עבודה הולכות לאיבוד בגלל compaction.

זה כלי שעושה דבר אחד: מוודא שעוזר ה-AI שלכם לפיתוח לא שוכח מה כבר בניתם. מתקינים פעם אחת, שוכחים מזה — הוא זוכר בשבילכם.

קוד פתוח. רישיון MIT. בחינם.

**GitHub:** [https://github.com/yan-yanko/rainman](https://github.com/yan-yanko/rainman)

```bash
pip install git+https://github.com/yan-yanko/rainman.git
rainman setup
rainman doctor
```

שלוש פקודות. דקה אחת. ואחרי זה — ה-AI שלכם סוף סוף זוכר.

</div>
